import 'package:flutter/material.dart';

class UnCompeletedPage extends StatelessWidget {
  const UnCompeletedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
