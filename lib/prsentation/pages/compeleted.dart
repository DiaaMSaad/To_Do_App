import 'package:flutter/material.dart';
import 'package:todo_app/prsentation/widgets/custom_text.dart';

class CompeletedPage extends StatelessWidget {
  const CompeletedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: CustomText(text: "Mohamed"));
  }
}
